var a: any = 'Ankit';
var b: string = 'Ankit';

var myName: string = 'Ankit';
var myAge: number = 29;
var mySkills = ['HTML5', 'CSS3', 29];

mySkills;

// Cannot assign different type of data to variable of a certain type
// myAge = '29';

// cannot use a variable if it is not available
// if (age > 18) {

// }

// Does give error compile time
if (myAge > 18) {
    let myNewName = 'Ankit';
}

//console.log(myNewName);

let superman = {
    name: 'Superman',
    power: 'Hanging'
};

superman;

// superman.name = 29;
// superman.age = 29;